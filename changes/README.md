# Design changes

Each active change describes one migration from the truth in `DESIGN.md` to a
new implemented truth. A change must state its boundary, invariants, migration,
deletions, tests, and exit criteria. Once complete, fold the result into
`DESIGN.md`; keep only changes that remain useful as migration history.

